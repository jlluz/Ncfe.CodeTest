namespace Ncfe.CodeTest
{
    public class ArchivedDataService : IArchivedDataService<ArchivedDataService>
    {
        public Learner GetArchivedLearner(int learnerId)
        {
            // retrieve learner from archive data service
            return new Learner();
        }
    }
}
